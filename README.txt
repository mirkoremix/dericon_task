Requirements:
For building and running the application you need:
	-JDK 1.8
	-Maven 3
	
	
Running the application:

To build application you can use the Spring Boot Maven plugin like so:

mvn clean install

and to run it:

mvn spring-boot:run
